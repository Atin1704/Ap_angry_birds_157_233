package com.data;

public class GameNotFound extends Exception {
    public GameNotFound() {
        super("Game not found");
    }
}

///link it with saved screen in a simple try catch block like a=a so that it doesn't trigger

